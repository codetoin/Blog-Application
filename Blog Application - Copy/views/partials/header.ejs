<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <title>Blog</title>
    <style>
      /* Body and Background */
      body {
        background-color: #f1f3f5; /* Soft gray background */
        color: #212529; /* Dark text for readability */
      }

      /* Navbar brand */
      .navbar-brand {
        background-color: #ffffff;
        color: #212529 !important;
        padding: 6px 10px;
        border-radius: 6px;
        font-weight: bold;
      }

      /* Active nav link */
      .nav-link.active {
        color: #0d6efd !important; /* Bootstrap primary blue */
      }

      /* Navbar hover links */
      .nav-link:hover {
        color: #0dcaf0 !important;
        transition: 0.2s;
      }

      /* Buttons hover */
      .btn:hover {
        opacity: 0.85;
        transition: 0.2s;
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
          <!-- Brand -->
          <!-- <a class="navbar-brand" href="/">BLOG</a> -->

          <!-- Mobile toggle -->
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Menu -->
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto">
                            <li class="nav-item">
                <a class="nav-link fs-5 active" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fs-5" href="/home">Read Blogs</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fs-5 active" href="/write">Write Blog</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fs-5" href="/about">About</a>
              </li>
            </ul>

            <!-- Buttons -->
            <div class="d-flex gap-2">
              <a class="btn btn-outline-primary btn-l" href="/signIn">Sign In</a>
              <a class="btn btn-primary btn-l" href="/register">Sign-up</a>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <!-- Main content placeholder -->
    <div class="page-wrapper" style="min-height: 100vh; display: flex; flex-direction: column;">
  <main class="flex-grow-1">
